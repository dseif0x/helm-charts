apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cliproxyapi.fullname" . }}
  labels:
    {{- include "cliproxyapi.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
