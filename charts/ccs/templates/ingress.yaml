apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-ccs-ingress
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    cert-manager.io/cluster-issuer: letsencrypt-dns
spec:
  tls:
    - hosts:
        - "{{ .Values.domain }}"
        - "{{ .Values.apidomain }}"
      secretName: ccs-cert
  rules:
    - host: {{ .Values.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ccs-svc
                port:
                  name: http
    - host: {{ .Values.apidomain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ccs-svc
                port:
                  name: cliproxy

